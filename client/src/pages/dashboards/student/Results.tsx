import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const Results = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>النتائج</CardTitle>
        <CardDescription>هنا ستجد تفاصيل نتائج امتحاناتك.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>سيتم عرض تقارير النتائج هنا قريبًا.</p>
      </CardContent>
    </Card>
  );
};

export default Results; 